# Terraform GCP Instance Launch

This repository deploys a Google Compute Engine VM using Terraform.

## Commands

terraform init  
terraform plan  
terraform apply -auto-approve  
terraform destroy -auto-approve  
